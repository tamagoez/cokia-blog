# OID
Onetime-IDの略です。  
主に、サーバーとBase同士でクライアントを介して、APIを使用する必要がある場合に発行されます。

## 仕様
BaseのOIDを発行するには、サーバー側で発行したOIDを登録する必要があります。

## 引数
もちろんfetcherで、IDとKeyをPOSTする必要があります。
### serverOID(必須)
サーバー側で発行したOIDを申請します。
### type(必須)
OIDをどのように利用するかを指定します。  
| type | 説明 |
| --- | --- |
| 1 | 新規登録 |
